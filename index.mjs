/** Host half for the browser-only conversation jump plugin. */
export const name = 'dsh-conversation-jump'

/** No host services, events, tools, or persistence are required. */
export function apply() {}
